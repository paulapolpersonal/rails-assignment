<%#OrderItem.all.each do |item| %>
<!--  <h3><%#= item.product.name %></h3>-->
<!--  <h5><%#= item.product.price %></h5>-->
  <%#=  form_with(model: @order_item, local: true) do |f| %>
    <%#= f.hidden_field :product_id, :value => item.product.id %>
    <%#= f.number_field :quantity, :value => item.quantity.to_i %>
    <%#= f.submit "Update Cart " %>
    <%#= link_to "Remove Items", order_item_path(item), method: :delete, remote: true %>
  <%# end %>
<!--  <h2>Subtotal: <%#= item.total %></h2>-->
<%# end %>

<!--<h3>Subtotal is: <%#= current_cart.subtotal %></h3>-->


<% OrderItem.all.each do |item| %>
  <div>
      <h4><b><%= item.product.name %></b></h4>
      <p>$<%= item.product.price %></p>
      <p><%= item.quantity %></p>
  </div>
  <%= link_to "Remove Item", @order_item, method: :delete,
              data: { confirm: "Are you sure you want to delete this item?" } %>
<%end %>